# Local dev artifacts
.tmp/
.legacy-rescue/

# Environment files (never commit credentials)
.env
.env.local
.env.*.local
backend/.env
frontend/.env.local

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
.coverage
coverage.xml
htmlcov/

# Node
node_modules/
dist/
.vite/

# IDE
.vscode/
.idea/
*.swp
*.code-workspace

# Agent / Claude Code personal state — not part of the shareable build
.agents/
.claude/
agent-control/

# Obsidian vault metadata (in case it appears anywhere)
.obsidian/

# ML model artifacts — too large for git; deployed via setup_models.py + scp/rsync
*.onnx
*.pt
*.pth
*.engine
*.plan
infra/models/

# Internal review snapshots — not part of the build
REVIEW-*.md

# Celery beat runtime state (re-created on each run)
celerybeat-schedule*

# graphify-mcp generated graph analysis (regenerable)
graphify-out/

